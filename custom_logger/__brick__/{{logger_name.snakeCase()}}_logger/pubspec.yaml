name: {{logger_name.snakeCase()}}_logger
description: A Custom Logger created by custom_logger brick.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  {{#use_crashlytics}}firebase_crashlytics: ^3.4.6{{/use_crashlytics}}
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  very_good_analysis: ^5.1.0
